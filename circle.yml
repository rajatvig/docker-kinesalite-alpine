machine:
  services:
    - docker

test:
  override:
    - docker run -it --rm -v "$PWD/Dockerfile":/Dockerfile:ro redcoolbeans/dockerlint

deployment:
  docker:
    branch: master
    commands:
      - make build
      - make tag
      - make push
